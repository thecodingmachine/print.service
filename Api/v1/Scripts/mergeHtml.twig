{% for htmlTemplate in htmlTemplates %}

        {% if htmlTemplate["header"] is not null %}

            {% include htmlTemplate["header"] %}

        {% endif %}

        {% include htmlTemplate["body"] %}

        {% if htmlTemplate["footer"] is not null %}

            {% include htmlTemplate["footer"] %}

        {% endif %}

{% endfor %}
